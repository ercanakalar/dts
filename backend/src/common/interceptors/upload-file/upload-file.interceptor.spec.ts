import { UploadFileInterceptor } from "./upload-file.interceptor";

describe("UploadFileInterceptor", () => {
    it("should be defined", () => {
        expect(new UploadFileInterceptor()).toBeDefined();
    });
});
